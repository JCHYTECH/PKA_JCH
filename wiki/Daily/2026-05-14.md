---
date: 2026-05-14
tags: [daily]
type: daily
status: active
---

## Actions

- Création du skill `/modele` (`~/.claude/skills/modele/SKILL.md`) — Q&A enrichissement contexte → prompt amélioré → recommandation modèle LLM + estimation tokens
- Intégration des benchmarks officiels Anthropic (MMLU, GPQA Diamond, HumanEval, SWE-bench, MATH, GSM8K) dans le skill `/modele` comme source objective de recommandation
- Attribution ownership `/modele` : Furet 🦡 conduit, Dobby 🦉 synthétise
- Trigger `/modele` ajouté dans `~/.claude/CLAUDE.md`
- Correction drift `CLAUDE.md` : Dobby emoji 🐶 → 🦉 (aligné sur `team.db`), Chouette #25 ajoutée au roster
- Création `PKA_JCH/MEMORY.md` — miroir portable de la mémoire accumulée, accessible depuis tout outil
- Référence `MEMORY.md` ajoutée dans `ADAPTER-PROMPT.md`, `AGENTS.md`, `GEMINI.md` comme 1ère lecture obligatoire
- Ajout champ `model: claude-sonnet-4-6` dans les 10 fichiers mémoire `~/.claude/memory/*.md`
- Création table `memory_log` dans `team.db` — traçabilité modèle par entrée mémoire
- Ajout colonne `model` dans table `skills` (team.db)
- Ajout bloc **Session Start Protocol** dans `CLAUDE.md` — identification modèle + chargement mémoire + tag automatique
- Ajout **Compliance check** dans `ADAPTER-PROMPT.md` — question de vérification pour sessions hors Claude Code
- Création `MEMORY-ARCHITECTURE.md` — documentation de l'architecture mémoire transversale
- Sauvegarde mémoire : `feedback_dobby_delegation.md` + `feedback_attribution_equipe.md`

## Décisions

- **Furet** est le propriétaire du skill `/modele`, pas Dobby — cohérence avec la règle de délégation maximale
- `team.db` est la source de vérité pour Dobby's animal : 🦉 Owl (pas 🐶 Chihuahua comme CLAUDE.md l'indiquait)
- La mémoire portable (`MEMORY.md`) vit dans `PKA_JCH/` racine, pas dans `~/.claude/` — accessibilité multi-outils
- Niveau de certitude différencié par outil : Claude Code (certaine) > Codex/Gemini CLI (élevée) > ChatGPT web (moyenne) > DeepSeek (faible)
- Compliance check en 3 points pour valider le chargement de contexte hors Claude Code

## Prochaine étape

- Tester `/modele` sur une vraie tâche
- Synchroniser `MEMORY.md` après chaque session significative
- Définir et formaliser le protocole "interview équipe" (évoqué mais non retrouvé en mémoire — à reconstruire avec JCH)

---

## Actions (session 2 — après-midi)

- Correction rôle Jade dans `team.db` et `jade.md` : ajout "& Legal" (dépendance de Renard pour les documents légaux)
- Projet NUANCES — travail sur le deck B2B Arteon × Nuances (20 slides)
- Furet : extraction liste de risques BESS depuis la base de connaissance
- Furet × Iris : analyse croisée risques BESS vs situation Nuances → 2 nouvelles slides (solutions + points à vérifier)
- Restructuration `analyse_nuances_b2b_mission.md` : 15 → 20 slides
- Génération PPTX via `generate_nuances_deck.py` (clone XML depuis `arteon-slide-masters.pptx`)
- Diagnostic et tentatives de correction des erreurs PPTX : IDs dupliqués, doublons ZIP, relations images manquantes
- Mémorisation directive versionnage : tout fichier modifié de façon significative → copie `_vN` avant édition

## Décisions (session 2)

- Maintien MT(?) dans slide ICP — à confirmer avec Nuances
- Liste KPI exhaustive conservée
- Slide 15 "Ce qui amplifie les 6 leviers" formalisée
- Directive versionnage fichiers adoptée suite à perte du script `generate_nuances_deck.py` par `sed` destructif

## Prochaine étape (session 2)

- Reconstruire `generate_nuances_deck.py` avec la correction des relations images (bug blipFill r:embed sans relation dans .rels)
- Tester le PPTX généré sans erreur PowerPoint
- Appliquer systématiquement la règle de versionnage sur tous les fichiers itératifs du projet NUANCES
