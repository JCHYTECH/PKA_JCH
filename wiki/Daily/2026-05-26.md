---
date: 2026-05-26
tags: [daily, procurement, wildnexus, mouser, api, forge]
type: daily
status: active
---

# 2026-05-26 — Agent Procurement — Implémentation + debug Cart API

## Actions

- Finalisation de l'agent procurement semi-autonome (`scripts/procurement/`)
  - `models.py` : 4 dataclasses (Component, EnrichedComponent, BOMData, ProcurementResult) + champ `found_mpn`
  - `config.py` : chargement `.procurement.env`, deux clés Mouser distinctes (Search + Cart)
  - `bom_parser.py` : parsing auto Excel / CSV / Markdown
  - `api_client.py` : MouserClient avec mode simulation, retry ×3, flags MANUEL / OBSOLETE
  - `llm_reasoner.py` : alternatives LLM pour composants obsolètes + narrative French
  - `bom_writer.py` : BOM enrichi Excel coloré (rouge=obsolète, jaune=simulation)
  - `report_writer.py` : rapport Markdown 7 sections en `TEAM_Inbox/`
  - `cart_client.py` : push panier Mouser + gestion 302 (IP non whitelistée)
  - `main.py` : CLI `--project / --bom / --output / --push-cart`
- Testé sur BOM WildNexus P0 v0.2 (16 composants) — 12/16 trouvés via Mouser API réelle
- Coût total estimé : 230.72 € — 3 composants marqués MANUEL
- 19 tests pytest : 100% pass
- Debug Cart API : endpoint corrigé `/api/v1/cart/items/insert` (doc officielle)
- Diagnostic 302 → Error.aspx : IP Mac non whitelistée pour Cart API (≠ Search API)
- Gestion `allow_redirects=False` + message d'erreur actionnable ajouté

## Décisions

- **Cart API depuis Mac** : bloquée par whitelist IP Mouser — lancer depuis RPi (tkajch.local)
- **Cart API key** : `13d4dc69...` est valide (GET retourne JSON), mais POST crashe côté Mouser si IP non whitelistée
- **Endpoint retenu** : `POST /api/v1/cart/items/insert` (confirmé par doc HTML `mouser.be/api-cart/`)
- **`found_mpn`** : ajouté dans EnrichedComponent — capture le MPN retourné par Mouser (peut différer du MPN BOM), utilisé pour le payload Cart
- **Clé `.procurement.env`** stockée sur Mac `~/.procurement.env` — à copier sur RPi pour déploiement

## Fichiers modifiés / commits

- `fix: Cart API endpoint /items/insert + gestion 302 IP non whitelistée` (ca1d4dc)

## Prochaine étape

1. Déployer sur RPi : `bash scripts/procurement/deploy_rpi.sh`
2. Copier `~/.procurement.env` sur RPi (`scp ~/.procurement.env jchavaux@tkajch.local:~/.procurement.env`)
3. Tester `--push-cart` depuis RPi pour valider la Cart API
4. Vérifier dans compte Mouser que l'IP du RPi est whitelistée pour la clé Cart (`13d4dc69...`)
